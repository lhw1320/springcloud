# 设置服务名称
spring:
  application:
      name: eurekaconsumer

# 设置端口号      
server:
  port: 2101

eureka:
  client:
    # Eureka注册中心地址
    service-url:
      defaultZone: "http://localhost:1001/eureka/"
        
        