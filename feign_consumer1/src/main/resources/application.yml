# 设置服务名称
spring:
  application:
      name: feignconsumer1

# 设置端口号      
server:
  port: 2104

eureka:
  client:
    # Eureka注册中心地址
    service-url:
      defaultZone: "http://localhost:1001/eureka/"
        
        