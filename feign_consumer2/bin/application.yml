# 设置服务名称
spring:
  application:
      name: feignconsumer2

# 设置端口号      
server:
  port: 2105

eureka:
  client:
    # Eureka注册中心地址
    service-url:
      defaultZone: "http://localhost:1001/eureka/"
        
        